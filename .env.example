# LLM Provider: 'groq' or 'ollama'
LLM_PROVIDER=groq

# Groq API Key (required if using Groq)
GROQ_API_KEY=your-groq-api-key-here

# Ollama settings (used if LLM_PROVIDER=ollama)
# Model can be changed in route.ts LLM_CONFIG
